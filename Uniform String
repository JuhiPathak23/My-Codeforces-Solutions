#include <bits/stdc++.h>
using namespace std;
int main() {
  int t;
  cin>>t;
  while(t--){
    int n,k;
    cin>>n>>k;
    string st(n,' ');
    for(int i=0;i<n;i++)
      st[i]='a'+(i%k);
    cout<<st<<endl;
  }
  return 0;
}


https://codeforces.com/problemset/problem/1092/A
