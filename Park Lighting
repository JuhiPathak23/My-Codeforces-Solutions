#include<bits/stdc++.h>
using namespace std;
int main(){
  int t;
  cin>>t;
  int n, m;
  while(t--){
    cin>>n>>m;
    int lights;
    lights = (n*m)%2 == 0 ? ((n * m) / 2) : (((n * m) / 2) + 1);
    cout<<lights<<endl;
  }
}

https://codeforces.com/problemset/problem/1358/A
